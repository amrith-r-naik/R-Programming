# R List is the object which contains elements of different types.
# R list can also contain a matrix or a function as its elements. The list is created using the list() function in R.

#Create a list
list1 <- list(c(2,5,3),21.3)
print(list1)
print(class(list1))